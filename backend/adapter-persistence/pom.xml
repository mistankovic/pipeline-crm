<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.pipelinecrm</groupId>
    <artifactId>pipelinecrm-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>adapter-persistence</artifactId>
  <name>PipelineCRM :: adapter-persistence</name>
  <description>Interface adapters for persistence. Implements application output ports with JPA.</description>

  <properties>
    <!--
      Opt-out, with reason. CONSTITUTION.md section 3 scopes the 95% coverage and CRAP
      gates to domain and application, which is where the business rules live. This
      module is an adapter: it is held to account by integration tests against a real
      database and by the architecture fitness functions, not by a line-coverage number.
      Checkstyle, CPD and the architecture rules still apply in full.
    -->
    <coverage.skip>true</coverage.skip>
    <crap.skip>true</crap.skip>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.pipelinecrm</groupId>
      <artifactId>application</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-crypto</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <!--
        The Postgres test harness is shared with the bootstrap module's integration tests.
        Publishing it as a test-jar keeps one copy rather than two that drift apart.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <executions>
          <execution>
            <id>test-harness</id>
            <goals><goal>test-jar</goal></goals>
            <configuration>
              <!--
                Publish the harness, not the tests. Shipping the whole test tree put this
                module's own @SpringBootApplication on the bootstrap module's test classpath,
                where component scanning found it and its beans collided with the real
                composition root. A shared artefact should contain exactly what is shared.
              -->
              <includes>
                <include>com/pipelinecrm/adapter/persistence/testing/**</include>
              </includes>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
