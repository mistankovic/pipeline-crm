<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.pipelinecrm</groupId>
    <artifactId>pipeline-crm</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>inner-parent</artifactId>
  <packaging>pom</packaging>
  <name>PipelineCRM Inner Layer Parent</name>
  <description>Quality gates and banned-dependency rules for domain and application</description>

  <properties>
    <crap.package.prefix>OVERRIDE_ME</crap.package.prefix>
    <pitest.target.classes>OVERRIDE_ME</pitest.target.classes>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.pipelinecrm</groupId>
      <artifactId>crap-check</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>ban-frameworks-on-inner-layers</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <bannedDependencies>
                  <searchTransitive>true</searchTransitive>
                  <message>Inner layers cannot depend on frameworks. See CONSTITUTION.md §2.</message>
                  <excludes>
                    <exclude>org.springframework:*</exclude>
                    <exclude>org.springframework.boot:*</exclude>
                    <exclude>org.springframework.data:*</exclude>
                    <exclude>org.springframework.security:*</exclude>
                    <exclude>org.hibernate.orm:*</exclude>
                    <exclude>org.hibernate:*</exclude>
                    <exclude>jakarta.persistence:*</exclude>
                    <exclude>jakarta.servlet:*</exclude>
                    <exclude>jakarta.ws.rs:*</exclude>
                    <exclude>com.fasterxml.jackson.core:*</exclude>
                    <exclude>com.fasterxml.jackson.datatype:*</exclude>
                    <exclude>org.projectlombok:lombok</exclude>
                  </excludes>
                </bannedDependencies>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>check-inner-coverage</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.95</minimum>
                    </limit>
                    <limit>
                      <counter>BRANCH</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.95</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>crap-gate</id>
            <phase>verify</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>java</executable>
              <classpathScope>test</classpathScope>
              <arguments>
                <argument>-classpath</argument>
                <classpath/>
                <argument>com.pipelinecrm.tools.crap.CrapGate</argument>
                <argument>--report</argument>
                <argument>${project.build.directory}/site/jacoco/jacoco.xml</argument>
                <argument>--threshold</argument>
                <argument>${crap.threshold}</argument>
                <argument>--classes-dir</argument>
                <argument>${project.build.outputDirectory}</argument>
                <argument>--package</argument>
                <argument>${crap.package.prefix}</argument>
              </arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>mutation</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <version>${pitest.version}</version>
            <dependencies>
              <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>${pitest.junit5.version}</version>
              </dependency>
            </dependencies>
            <executions>
              <execution>
                <id>mutation-coverage</id>
                <phase>verify</phase>
                <goals>
                  <goal>mutationCoverage</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <targetClasses>
                <param>${pitest.target.classes}</param>
              </targetClasses>
              <failWhenNoMutations>false</failWhenNoMutations>
              <mutationThreshold>100</mutationThreshold>
              <coverageThreshold>95</coverageThreshold>
              <timestampedReports>false</timestampedReports>
              <outputFormats>
                <param>HTML</param>
                <param>XML</param>
              </outputFormats>
              <excludedClasses>
                <param>*.package-info</param>
              </excludedClasses>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
